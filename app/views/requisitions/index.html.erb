<%WbWeek.where(school_id: current_user.school_id).all.each do |wb|%>

<%if Date.today < wb.wb.week_beginning && Date.today + 1.year%>
Add Requisition - <%= link_to wb.wb.week_beginning.strftime("%d %B, %Y") , add_requisition_requisitions_path(wb_id: wb.wb.id) %>
-
Week <%= wb.week_id%>

<br/>

<%end%>
<%end%>
